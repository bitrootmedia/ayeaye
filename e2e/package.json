{
  "name": "e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Browser tests. Deliberately its own package: apps/web's production image runs `pnpm install --frozen-lockfile`, which installs devDependencies, so Playwright living there would ship browser tooling in the deployed bundle.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
